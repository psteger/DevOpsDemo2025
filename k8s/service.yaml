apiVersion: v1
kind: Service
metadata:
  name: fiber-app-service
  namespace: fiber-app
  labels:
    app: fiber-app
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: fiber-app